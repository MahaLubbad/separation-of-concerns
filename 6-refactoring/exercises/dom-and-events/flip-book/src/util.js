/**
 *
 */
export const reverseAndUpper = (text) => text.toUpperCase()
                                            .split('')
                                            .reverse()
                                            .join('');
